// Try to import "someVar" here, it only suggests the relative import
// because of `.vscode/settings.json`.
// I believe it would make more sense to offer the absolute path, since the 
// module is declared in tsconfig.json "paths"
